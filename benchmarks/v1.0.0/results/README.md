# Recorded benchmark snapshot

> **Historical v1.0 benchmark:** retained for reproducibility; see the [current v1.1 report](../../../docs/v1.1/benchmarks.md).

`data/` contains the CSV files used by the benchmark READMEs. `figures/` contains matching PNG and SVG charts. The files are a single-machine snapshot and should be regenerated after material scheduler, compiler, dependency, or hardware changes.
