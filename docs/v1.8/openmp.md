# OpenMP status

OpenMP is not a v1.8 release requirement and no new OpenMP backend is added.

The release does not claim operation-scoped control of arbitrary OpenMP regions, compatibility between multiple OpenMP runtimes, or deterministic OpenMP team identity. Any future backend must be qualified separately for MSVC, GCC, LLVM, and intentionally supported Intel runtime combinations.
